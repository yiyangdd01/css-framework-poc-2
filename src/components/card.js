import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div.attrs(() => ({
    className: 'bg-black w-320 h-120'
}))


const ContentCard = () => {
    return (
        <Wrapper></Wrapper>
    )
}

export default ContentCard;